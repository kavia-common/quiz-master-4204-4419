# Quiz Frontend (.env.example)
# Copy this file to .env and set the backend API base URL.
# This must be a full origin with optional path prefix, no trailing slash.

# During local development, FastAPI typically runs on 8000.
REACT_APP_API_BASE_URL=http://localhost:8000
